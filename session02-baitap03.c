#include <stdio.h>

int main() {
	int a = 10;
	int b = 5;
	int c = a + b;
    int d = a - b;
    int e = a * b;
    float f = a / b;

printf ("tong cua a va b la : %d", c);

printf ("hieu cua a va b la : %d", d);

printf ("tich cua a va b la : %d", e);

printf ("thuong cua a va b la : %f", f);

return 0;
}
